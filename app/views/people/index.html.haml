- @people_page = true
.headerbw
  = link_to 'New Person', new_person_path, class: 'btn btn-bw btn-lg'

.people
  - @people.each do |p|
    = link_to person_path(p) do
      .person
        .grade= p.last_response_grade
        .name= p.full_name
