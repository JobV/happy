%h1 People
= link_to 'New Person', new_person_path, class: 'btn btn-default btn-lg'
%table.table
  %thead
    %tr
      %th First name
      %th Last name
      %th Birthdate
      %th Email
      %th Phone
      %th{:colspan => "3"}
  %tbody
    - @people.each do |person|
      %tr
        %td= person.first_name
        %td= person.last_name
        %td= person.birthdate
        %td= person.email
        %td= person.phone
        %td= link_to 'Show', person
        %td= link_to 'Edit', edit_person_path(person)
        %td= link_to 'Destroy', person, method: :delete, data: { confirm: 'Are you sure?' }
%br/
