- @conversations_page = true
.headerbw
  = link_to 'Query Happiness', query_happiness_path, class: 'btn btn-bw btn-lg', remote: true, method: :post

= column_chart Response.group(:grade).count

- if @responses.count > 0
  %table.table
    %thead
      %tr
        %th Body
        %th Grade
        %th Person
        %th{ colspan: '3' }

    %tbody
      - @responses.each do |response|
        %tr
          %td= response.body
          %td= response.grade
          %td= response.person.first_name if response.person && response.person.first_name
          %td= link_to 'Show', response
          %td= link_to 'Destroy', response, method: :delete
- else
  Nothing here
