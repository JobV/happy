.headerbw
  %h1= "#{@response.person.first_name}'s response on #{@response.created_at.strftime("%B %e")}"


%p#notice= notice

= "#{@response.person.first_name} rated #{@response.grade} on #{@response.created_at.strftime("%B %e")}"
%h2= @response.grade

%p= @response.body
