.ui.page.grid
  .column
    .ui.center.aligned.basic.segment
      .ui.statistic
        .value
          = @people.count
        .label
          People are happy!

    %table.ui.table
      %thead
        %tr
          %th Name
          %th action required
          %th.right.aligned status
      %tbody
        - @people.each do |p|
          %tr
            %td= link_to p.full_name, person_path(p)
            %td
            %td.right.aligned
              .ui.green.label
                Good

      %tfoot
        %tr.full-width.active
          %th
          %th{ colspan: '4' }
            = link_to new_person_path, class: "ui right floated primary button" do
              Add User
